const box = {
  fontFamily: "'Inter', sans-serif",
  padding: '0.5rem 0.5rem',
};

const gray_text = {
  fontWeight: 400,
  color: 'gray.500',
  paddingBottom: '1',
};

const main_text = {
  fontWeight: 500,
  paddingBottom: '1',
};

const admin_text = {
  fontWeight: 700,
}

const button = {
  colorScheme : 'purple',
  variant : 'outline',
  mt : '3',
  width: '48'
};

const accordionpanel = {
  width: '20vw',
  border:0,
}

const accordion = {
  borderTop : 'px #fff',
  borderBottom :'px #fff',
}


const styles = {
  box,
  gray_text,
  main_text,
  admin_text,
  button,
  accordionpanel,
  accordion
};

export default styles;
