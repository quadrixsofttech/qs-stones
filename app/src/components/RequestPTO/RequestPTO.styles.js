const box = {
  fontFamily: "'Inter', sans-serif",
  p: '2',
};

const gray_text = {
  fontWeight: 400,
  color: 'gray.700',
  fontSize: 'sm',
  paddingBottom: '1',
};

const main_text = {
  fontWeight: 500,
  paddingBottom: '1',
  fontSize: 'md',
};

const admin_text = {
  fontWeight: 700,
  fontSize: 'md',
};

const button = {
  colorScheme: 'purple',
  variant: 'outline',
  mt: '3',
  width: '48',
};

const accordionpanel = {
  width: '18vw',
  border: 0,
};

const accordion = {
  borderTop: 'px #fff',
  borderBottom: 'px #fff',
};

const text_area_panel = {
  mt: '2',
  width: '200',
  height: '200',
  fontSize: '0.9rem',
};

const styles = {
  box,
  gray_text,
  main_text,
  admin_text,
  button,
  accordionpanel,
  accordion,
  text_area_panel,
};

export default styles;
