const progress = {
    alignItems: 'center',
    gap: 2,
    mt: 2,
  };
  
  const textRemote = {
    fontSize: '2rem',
    fontFamily: 'Inter',
    fontWeight: '700',
  };
  
  const textRequestDates = {
    fontFamily: 'Inter',
    mt: 3,
    ml: 1,
  };
  
  const flexTag = {
    p: 3,
    alignItems: 'center',
    gap: 3,
    mb: 2,
  };

  const flexError ={
    bg:'red.100',
    height:'10',
    alignItems:'center',
    justifyContent:'center'
  }

  const textError = {
    color:'red.800',
    fontSize:'15'
  }

  const flexSpinner = {
    alignItems:'center',
    justifyContent:'center'
  }
  


const styles = {
    progress,
    textRemote,
    textRequestDates,
    flexTag,
    flexError,
    textError,
    flexSpinner
}

export default styles;