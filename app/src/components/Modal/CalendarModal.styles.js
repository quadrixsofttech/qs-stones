const progress = {
    alignItems: 'center',
    gap: 2,
    mt: 2,
  };
  
  const textRemote = {
    fontSize: '2rem',
    fontFamily: 'Inter',
    fontWeight: '700',
  };
  
  const textRequestDates = {
    fontFamily: 'Inter',
    mt: 3,
    ml: 1,
  };
  
  const flexTag = {
    p: 3,
    alignItems: 'center',
    gap: 3,
    mb: 2,
  };
  


const styles = {
    progress,
    textRemote,
    textRequestDates,
    flexTag
}

export default styles;