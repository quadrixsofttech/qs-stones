const container = {
  width:'25%',
  borderWidth: '1px',
  borderRadius: '6px',
  p: '0px',
  border: '1px solid #E2E8F0',
};

const tabindicator = {
  mt: '-1.5px',
  height: '2px',
  bg: 'purple.500',
  borderRadius: '1px',
};

const flex = {
  bg: 'blackAlpha.50',
  w: '100%',
  h: '60px',
  color: 'black',
  p: '2',
  alignItems: 'center',
};

const tabs = {
  position: 'relative',
  variant: 'unstyled',
  mt: '2',
};

const tabpanel = {
  maxHeight:'60vh',
  overflowY: 'auto' 
}

const styles = {
  container,
  tabindicator,
  flex,
  tabs,
  tabpanel
};

export default styles;
