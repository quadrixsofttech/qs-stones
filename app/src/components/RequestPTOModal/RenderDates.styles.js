const textRemote = {
  fontSize: '2rem',
  fontFamily: 'Inter',
  fontWeight: '700',
};

const textRequestDates = {
  fontFamily: 'Inter',
  mt: 3,
  ml: 1,
};

const flexTag = {
  p: 3,
  alignItems: 'center',
  gap: 3,
  mb: 2,
};

const flexError = {
  bg: 'red.100',
  height: '10',
  alignItems: 'center',
  justifyContent: 'center',
};

const textError = {
  color: 'red.800',
  fontSize: '15',
};

const flexSpinner = {
  alignItems: 'center',
  justifyContent: 'center',
};

const icon = {
  fontSize: 'lg',
  mt: '2',
  color: 'gray.400',
};

const rangeTag = {
  size: 'sm',
  fontSize: '12px',
  borderRadius: '6px',
  colorScheme: 'gray',
  marginRight: '2',
  marginTop: '1',
};

const styles = {
  icon,
  textRemote,
  textRequestDates,
  flexTag,
  flexError,
  textError,
  flexSpinner,
  rangeTag,
};

export default styles;
