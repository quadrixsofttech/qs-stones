import { Box } from '@chakra-ui/react';
import styles from './RequestDenied.styles';

const RequestDenied = () => {
  return <Box {...styles.box_rejected}>REJECTED</Box>;
};

export default RequestDenied;
