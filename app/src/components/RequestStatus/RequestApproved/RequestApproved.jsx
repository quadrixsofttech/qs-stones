import { Box } from '@chakra-ui/react';
import styles from './RequestApproved.styles';

const RequestApproved = () => {
  return <Box {...styles.box_approved}>APPROVED</Box>;
};

export default RequestApproved;
