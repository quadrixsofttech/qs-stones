import { Box } from '@chakra-ui/react';
import styles from './RequestPending.styles';

const RequestPending = () => {
  return <Box {...styles.box_pending}>PENDING</Box>;
};

export default RequestPending;
